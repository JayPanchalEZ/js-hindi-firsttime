"use strict"; //treat all js code as newer version of JS

//alert(3+3)  //we're using nodejs not browser so it'll not work here..

//code readability should be high

let name = "Jay"  //String
let age = 20 //Integer
let isLoggedIn = false //boolean

// number => 2 to pwer 53
// bigint
// String => ""
// Boolean => true/false
// null (a standalone value or type) representation of empty value, here we're assigning value as null
// undefined means no value or no datatype assigned to that variable
// symbol => to find uniqueness/ unique

// object

console.log(typeof null); //object
console.log(typeof undefined); // undefined